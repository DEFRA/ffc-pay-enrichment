# Pipeline name convention
name: '$(Build.SourceBranchName)$(System.PullRequest.SourceBranch)$(Rev:.r)'

# Trigger configuration
trigger:
  - main
  - feature/*
  - release/*

pr:
  - main
  - releases/*

# Parameters
parameters:
  - name: prDeployment
    type: boolean
    default: true
  - name: branchDeployment
    type: boolean
    default: true
  - name: sndDeployment
    type: boolean
    default: true

# Resources
resources:
  repositories:
    - repository: templates
      endpoint: DEFRA
      type: github
      name: defra/fcp-pipeline-common
      # ref: pl-709   # Optional: build specific pipeline branch

# Pipeline template
extends:
  template: fcp-common-pipeline.yaml@templates
  parameters:
    appFrameworkType: nodejs   # Optional: nodejs (default), dotnet, java
    Type: kubernetes           # Optional: kubernetes (default), npm, functions, nuget
    npmLegacyPeerDeps: false   # Optional: default is false
    dockerFilePath: Dockerfile # Optional default is Dockerfile
    failOnThreshold: moderate  # Optional: moderate (default), low, high, critical
    prDeployment: ${{ parameters.prDeployment }}
    branchDeployment: ${{ parameters.branchDeployment }}
    sndDeployment: ${{ parameters.sndDeployment }}
    variableGroupName: ffc-pay-enrichment-env-var
    pr_params:
      container.processingActive: $(pr_processingActive)
      container.activeSchemes.ahwr: $(pr_activeSchemesAhwr)
      container.activeSchemes.fptt: $(pr_activeSchemesFptt)
    snd2_params:
      container.processingActive: $(snd2_processingActive)
      container.activeSchemes.ahwr: $(snd2_activeSchemesAhwr)
      container.activeSchemes.fptt: $(snd2_activeSchemesFptt)
    dev_params:
      container.processingActive: $(dev_processingActive)
      container.activeSchemes.ahwr: $(dev_activeSchemesAhwr)
      container.activeSchemes.fptt: $(dev_activeSchemesFptt)
    test_params:
      container.processingActive: $(test_processingActive)
      container.activeSchemes.ahwr: $(test_activeSchemesAhwr)
      container.activeSchemes.fptt: $(test_activeSchemesFptt)
    pre_params:
      container.processingActive: $(pre_processingActive)
      container.activeSchemes.ahwr: $(pre_activeSchemesAhwr)
      container.activeSchemes.fptt: $(pre_activeSchemesFptt)
    prd_params:
      container.processingActive: $(prd_processingActive)
      container.activeSchemes.ahwr: $(prd_activeSchemesAhwr)
      container.activeSchemes.fptt: $(prd_activeSchemesFptt)
