const enrichmentConfig = require('./enrichment')
const messageConfig = require('./messaging')
const databaseConfig = require('./database')

module.exports = {
  enrichmentConfig,
  messageConfig,
  databaseConfig
}
