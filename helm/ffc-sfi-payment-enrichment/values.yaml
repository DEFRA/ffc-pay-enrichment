environment: development
name: ffc-pay-enrichment
namespace: ffc-pay
workstream: ffc-pay
image: ffc-pay-enrichment

labels:
  component: service

appInsights:
  key:

containerConfigMap:
  name: ffc-pay-enrichment-config-map

containerSecret:
  name: ffc-pay-enrichment-container-secret
  type: Opaque

deployment:
  restartPolicy: Always
  priorityClassName: default
  replicas: 1
  minReplicas: 1
  maxReplicas: 4
  cpuThreshold: 80
  memoryThreshold: 80
  minReadySeconds: 0

container:
  imagePullPolicy: IfNotPresent
  requestMemory: 120Mi
  requestCpu: 40m
  limitMemory: 180Mi
  limitCpu: 120m
  messageQueueHost: namespace.servicebus.windows.net
  paymentTopicAddress: ffc-sfi-payment-request
  paymentSubscriptionAddress: ffc-sfi-payment-request-enrichment
  processingTopicAddress: ffc-sfi-payment-processing

postgresService:
  postgresDb: ffc_sfi_payment_enrichment
  # postgresExternalName is the external host name to which PostgreSQL
  # requests should be forwarded. If empty, PostgreSQL is assumed to be
  # within the cluster and accessible via postgresHost
  postgresExternalName:
  postgresHost: ffc-sfi-payment-enrichment-postgres
  postgresPort: 5432
  postgresSchema: public
  postgresUser: postgres

aadPodIdentity: true

azureIdentity:
  clientID: not-a-real-clientID
  resourceID: not-a-real-resourceID
